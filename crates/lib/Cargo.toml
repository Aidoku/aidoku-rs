[package]
name = "aidoku"
version = "0.1.0"
edition = "2021"
publish = true

[dependencies]
aidoku_imports = { path = "../imports" }
aidoku_codegen = { path = "../codegen" }
wee_alloc = { version = "0.4.5", optional = true }

[features]
default = ["wee_alloc"]
